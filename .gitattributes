# 저장소에는 항상 LF로 저장한다.
# Prettier가 LF로 쓰고 Windows 체크아웃이 CRLF로 되돌리면서 파일이 영구히
# "수정됨"으로 보이는 잡음을 막는다.
* text=auto eol=lf

# 바이너리
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
