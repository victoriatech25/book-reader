# 에이전트 작업 지침

## 먼저 읽을 것

| 문서                                 | 용도                                                          |
| ------------------------------------ | ------------------------------------------------------------- |
| [docs/PRD.md](docs/PRD.md)           | 기획 — 데이터 모델, 기능 명세, 화면 설계. 구현 판단의 근거    |
| [docs/WORKPLAN.md](docs/WORKPLAN.md) | 작업 단위(W), 검증 레벨(V1–V5), 컨펌 게이트(G0–G5), 실행 규약 |

작업을 시작하기 전에 **자기 작업 단위(W번호)의 행과 PRD의 해당 절**을 읽는다. 세션 간 인수인계는 대화 기억이 아니라 이 두 문서로만 이뤄진다.

## 작업 규약 (요약 — 전문은 WORKPLAN §6)

- 1 단위 = 1 브랜치(`feat/W<번호>-<슬러그>`) = 리뷰 가능한 커밋 묶음.
- 선행 단위의 산출물이 실제로 존재하는지 확인한다. 끝났다는 말을 믿지 않는다.
- 완료 시 DoD 검증 결과를 **실제 명령 출력과 함께** 보고한다.

### 중단하고 물어볼 것

스키마 변경 · PRD에 없는 기능 · 새 런타임 의존성 추가 · `.env.local` 값 부재 · 외부로 나가는 행위(배포·메일 발송).

### 금지

- 테스트를 통과시키려고 단정을 약화시키거나 스킵하지 않는다. 실패하면 실패 출력을 붙여 보고한다.
- 키 값을 코드·커밋·로그에 넣지 않는다. `.env.local`은 커밋 대상이 아니다.
- 마이그레이션을 콘솔 SQL 에디터에서 직접 실행하지 않는다. 항상 `supabase/migrations/` 파일로 관리한다.

## 명령

```bash
pnpm dev           # 개발 서버
pnpm verify        # typecheck + lint + test + build  (커밋 전 실행)
pnpm test          # Vitest 단위 테스트
pnpm test:e2e      # Playwright E2E
pnpm format        # Prettier
```

## 구조

```
src/app/          라우트 (App Router)
src/components/   UI 컴포넌트
src/lib/          순수 로직 · 클라이언트 팩토리
supabase/         마이그레이션
tests/unit/       Vitest
tests/e2e/        Playwright
docs/             기획서 · 작업계획서
```

순수 로직(상태 전이 규칙, 진행률 계산, 통계 집계)은 `src/lib/`에 분리해 단위 테스트 대상으로 만든다. 컴포넌트에 계산을 묻어두지 않는다.
